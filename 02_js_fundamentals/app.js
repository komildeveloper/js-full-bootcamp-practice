'use strict';

/* const myName = 'Komil';
let mySurname = false;

// if (myName) mySurame = true; // boommmmmmm !!!!!!! error
if (myName) mySurname = true; // yessss !!!!! true it's working
console.log(mySurname); // true */

// FUNCTIONS
// INTRO

/* function myName() {
  console.log('My name is Komil');
}

// calling / running / invoking function

myName();
myName();
myName(); */

function myCars(bmw, mercedes) {
  return `I have ${bmw} BMW and ${mercedes} Mercedes-Benz, Are you?`;
}

const myNewCars = myCars(2, 5);
console.log(myNewCars);
